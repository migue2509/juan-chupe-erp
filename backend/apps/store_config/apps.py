from django.apps import AppConfig


class StoreConfigConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name               = 'apps.store_config'
    verbose_name       = 'Configuración de tienda'
